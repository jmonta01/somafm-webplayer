'use strict';

angular.module('somafmPlayerApp')
  .factory('CommunityService', [
    '$http', '$log', '$q', 'AppURLs', 'StationTransform', 'PlsTransform', 'PlaylistTransform',
    function ($http, $log, $q, AppURLs, StationTransform, PlsTransform, PlaylistTransform) {

      return {
      }
    }
  ]);
